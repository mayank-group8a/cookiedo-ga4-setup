{% section 'banner-404' %}

{% section 'feature-columns-404' %}

<div class="page-width">
  <div class="grid">
    <div class="grid__item medium-up--five-sixths medium-up--push-one-twelfth">
      <!-- <div class="section-header text-center">
        <h1>{{ page.title }}</h1>
      </div> -->
	<div class="container-4 w-container">
      <div class="rte">
        {{ page.content }}
      </div>
    </div>
  </div>
</div>
</div>

