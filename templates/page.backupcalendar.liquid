<style>
  
  li, li a, .product-single__title, .hidden-text, .hidden-text p, .product-single__description p, h5 {
    font-family: 'Poppins';
  }
  
  li a {
    font-weight: 400 !important;
  }
  
</style>

{% section 'header-featured-banner' %}

{% include 'hypervisual' %}

<div id="Team" class="{{ page.handle }} content-section content-pages">
  <div class="content-wrapper w-container">
    <!-- <div class="section-header text-center">
<h1>{{ page.title }}</h1>
</div> -->

    <div class="rte">
      {{ page.content }}
    </div>
  </div>
</div>


<!--

					 
					  if(currentDate == '1' && get_hour < 12){
						  delivery_val = delivery_val + 1;
                      }else if(currentDate == '1' && get_hour > 12){
                         delivery_val = delivery_val + 2;
                      }else if(currentDate == '2' && get_hour < 12){
                          delivery_val = delivery_val + 1;
                      }else if(currentDate == '2' && get_hour > 12 && delivery_val == '1'){
						  delivery_val = delivery_val + 2;
					  }else if(currentDate == '2' && get_hour > 12 && delivery_val == '2'){
						  delivery_val = delivery_val + 6;
                      }else if(currentDate == '3' && get_hour < 12 && delivery_val == '1'){
                          delivery_val = delivery_val + 1;
                      }else if(currentDate == '3' && get_hour > 12 && delivery_val == '1'){
                          delivery_val = delivery_val + 4;
                      }else if(currentDate == '3' && get_hour < 12 && delivery_val == '2'){
                          delivery_val = delivery_val + 5;
					  }else if(currentDate == '3' && get_hour > 12){
						  delivery_val = delivery_val + 5;
					  }else if(currentDate == '4'){
						  delivery_val = delivery_val + 4;
					  }else if(currentDate == '5'){
						  delivery_val = delivery_val + 3;
					  }else if(currentDate == '6'){
						  delivery_val = delivery_val + 2;
					  }else if(currentDate == '7' && get_hour < 12){
						  delivery_val = delivery_val + 1;
                      }else if(currentDate == '7' && get_hour > 12){
						  delivery_val = delivery_val + 2;
					  }

-->

